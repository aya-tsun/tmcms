services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - tmcms-data:/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    restart: unless-stopped

volumes:
  tmcms-data:
